(function(g){'use strict';function E(c){let e=Error(c);e.code=c;throw e}function create(cols){if(!Array.isArray(cols)||!cols.length)E('COLUMNS_REQUIRED');let rows=[];let selected=new Set();function add(row){const r={...row};rows.push(r);return Object.freeze({...r})}function replace(next){if(!Array.isArray(next))E('ROWS_REQUIRED');rows=next.map(r=>({...r}));selected=new Set([...selected].filter(id=>rows.some(r=>String(r.__id)===id)));return snapshot()}function sortBy(k,dir){if(!cols.includes(k))E('UNKNOWN_COLUMN');const mul=dir==='desc'?-1:1;rows=rows.slice().sort((a,b)=>String(a[k]??'').localeCompare(String(b[k]??''),'de',{numeric:true})*mul);return snapshot()}function filter(k,v){if(!cols.includes(k))E('UNKNOWN_COLUMN');return Object.freeze(rows.filter(r=>String(r[k]??'').toLowerCase().includes(String(v).toLowerCase())).map(r=>Object.freeze({...r})))}function filterAny(v){const q=String(v??'').toLowerCase();return Object.freeze(rows.filter(r=>cols.some(k=>String(r[k]??'').toLowerCase().includes(q))).map(r=>Object.freeze({...r})))}function select(id,on=true){id=String(id);on?selected.add(id):selected.delete(id);return selection()}function clearSelection(){selected.clear();return selection()}function selection(){return Object.freeze([...selected])}function snapshot(){return Object.freeze(rows.map(r=>Object.freeze({...r})))}return Object.freeze({version:'1.1.0',add,replace,sortBy,filter,filterAny,select,clearSelection,selection,snapshot})}g.TableCore=Object.freeze({version:'1.1.0',apiVersion:'1.1',create});})(typeof window!=='undefined'?window:globalThis);